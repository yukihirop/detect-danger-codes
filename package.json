{
  "name": "detect-danger-codes",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "bin": {
    "ddc": "./packages/core/bin/ddc"
  },
  "scripts": {
    "build": "yarn workspaces run build",
    "eg:js": "yarn workspace @detect-danger-codes/core run eg:js",
    "eg:ts": "yarn workspace @detect-danger-codes/core run eg:ts",
    "test": "yarn workspaces run test"
  }
}
